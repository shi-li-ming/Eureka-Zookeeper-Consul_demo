server:
  port: 80

#服务名字
spring:
  application:
    name: consul-consumer-order
    #注册服务
  cloud:
    consul:
      host: localhost
      discovery:
        service-name: ${spring.application.name}
      port: 8500



